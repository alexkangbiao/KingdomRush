#ifndef __WELCOME_SCENE_H__#define __WELCOME_SCENE_H__#include "cocos2d.h"#include "SaveMenu.h"USING_NS_CC;// example for Sprite Kit, for Cocos2d simply inverse the order of the values!enum WelcomeSceneDrawingOrder : unsigned int {    WelcomeSceneDrawingOrderBackground = 0,    WelcomeSceneDrawingOrderCredit,    WelcomeSceneDrawingOrderStart,    WelcomeSceneDrawingOrderLogo,    WelcomeSceneDrawingOrderButton,    WelcomeSceneDrawingOrderLogoAnimation,    WelcomeSceneDrawingOrderSaveMenu};class WelcomeScene : public Layer, public OperateDelegate{public:    // there's no 'id' in cpp, so we recommend returning the class instance pointer    static cocos2d::Scene* createScene();    // Here's a difference. Method 'init' in cocos2d-x returns bool, instead of returning 'id' in cocos2d-iphone    virtual bool init();    // implement the "static create()" method manually    CREATE_FUNC(WelcomeScene);        void onStartOperate();    private:	//生成背景图	void initBackgroundImage();    void initMemuButton();    void initLogoImage();	void initStartButton();    void initCreditButton();    void initSaveMenu();        // For keyboard listener    void addKeyBoardListener();    void onKeyReleased(EventKeyboard::KeyCode keycode, Event* event);    	//生成&播放Logo动画	void startLogoAnimation();	void startStartButtonAnimation();    void startCreditButtonAnimation();	Size _visibleSize;	//logo	Sprite *_logoSprite;    Point _logoLocation;    	//按键	Sprite * _startButton;	Sprite * _creditButton;        SaveMenu *_saveMenu;    	void onEnterTransitionDidFinish();    void onExit();        //for button touch function    bool onStartTouchBegan(Touch *touch, Event *event);    void onStartTouchEnded(Touch *touch, Event *event);    bool onCreditTouchBegan(Touch *touch, Event *event);    void onCreditTouchEnded(Touch *touch, Event *event);};#endif