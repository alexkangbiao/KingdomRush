#ifndef _TOWER_PANLE_LAYER_H_#define _TOWER_PANLE_LAYER_H_#include "cocos2d.h"#include "Circle.h"#include "Terrain.h"#include "BaseBuildIcon.h"#include "BasePanelLayer.h"USING_NS_CC;class TowerPanelLayer: public BasePanelLayer{public:    CC_SYNTHESIZE(Terrain*, _terrain, MyTerrain);        virtual bool init() override;    CREATE_FUNC(TowerPanelLayer);private:    Circle* _circle;    Sprite* _temporaryTower;    bool    _isBuilding;        BaseBuildIcon* archerIcon;    BaseBuildIcon* artilleryIcon;    BaseBuildIcon* barracksIcon;    BaseBuildIcon* magicIcon;	virtual void addIcons() override;	void addTemporaryTower(int type);	void addTower(int type);        // 重载触摸回调函数    bool onBuildIconTouchBegan(Touch *touch, Event *event);    void onBuildIconTouchEnded(Touch* touch, Event* event);};#endif