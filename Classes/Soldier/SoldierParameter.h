#ifndef _SoldierParameter_H_#define _SoldierParameter_H_#include "cocos2d.h"USING_NS_CC;class SoldierParameter: public Node {public:    static SoldierParameter* createWithData(int type,                                            std::string name,                                            std::string baseImageName,                                            int level,                                            int maxHP,                                            int force,                                            int armor,                                            int speed,                                            int scope,                                            float attackPeriod,                                            float curePeriod,                                            float curePercentage);        CC_SYNTHESIZE(int, _type, Type);//保存战士类型    CC_SYNTHESIZE(std::string, _name, Name);//name    CC_SYNTHESIZE(std::string, _baseImageName, BaseImageName);//name    CC_SYNTHESIZE(int, _level, Level);//level    CC_SYNTHESIZE(int, _maxHP, MaxHP);//HP    CC_SYNTHESIZE(int, _force, Force);//force    CC_SYNTHESIZE(int, _armor, Armor);//armor    CC_SYNTHESIZE(int, _speed, Speed);//speed    CC_SYNTHESIZE(int, _scope, Scope);//scope    CC_SYNTHESIZE(float, _attackPeriod, AttackPeriod);//AttackPeriod    CC_SYNTHESIZE(float, _curePeriod, CurePeriod);          //士兵自愈间隔时间    CC_SYNTHESIZE(float, _curePercentage, CurePercentage);  //士兵每次恢复的比例（百分比）};#endif