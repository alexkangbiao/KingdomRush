#ifndef _PathHint_H_#define _PathHint_H_#include "cocos2d.h"USING_NS_CC;class PathHint : public Sprite{public:	PathHint();	~PathHint();    virtual bool init();    static PathHint* createPathHint(std::vector<Point> points);        void playMovingAnimation();private:    CC_SYNTHESIZE(std::vector<Point>, _pointsVector, PointsVector);    //行进路线        int _currentPointCounter;    Point getCurrentPoint();        Point getNextPoint();    Point getNextPoint(int stepNumber);    Point getAndMoveToNextPoint();    Point getAndMoveToNextPoint(int stepNumber);};#endif