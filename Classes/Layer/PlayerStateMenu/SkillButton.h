#ifndef _SkillButton_H_#define _SkillButton_H_#include "cocos2d.h"USING_NS_CC;enum SkillButtonDrawingOrder : unsigned int {    SkillButtonDrawingOrderBackgroundImage = 0,    SkillButtonDrawingOrderProgressImage,    SkillButtonDrawingOrderSelectedImage};class SkillButton : public Node{public:    CC_SYNTHESIZE(std::string, _state, State);    CC_SYNTHESIZE(bool, _isReady, Ready);    CC_SYNTHESIZE(bool, _isSelect, IsSelect);    CC_SYNTHESIZE(float, _dtTime, DTTime);        SkillButton();    virtual ~SkillButton();        /** 创建一个SkillButton对象 */    static SkillButton* createSkillButton(float dtTime,                                          const char* stencil_file_name,                                          const char* button_normal_name,                                          const char* button_click_name);        void startSkillButton();    void setCallback(const ccMenuCallback& callback);    void setEnable(bool enable);        Size getSkillButtonSize();    void unSelected();    private:    bool init(float dtTime, const char* stencil_file_name, const char* button_normal_name, const char* button_click_name);        ccMenuCallback   _callback;    Sprite*          _normalSprite;    Sprite*          _selectedSprite;    MenuItemImage*   _skillMenuItem; // 技能按钮    ProgressTimer*   _progressTimer; // 时间进度条精灵(360度旋转)        void  onTouchClickCallBack(Ref* pSender);    void  onCountDownFinishCallBack(Ref* pSender);};#endif