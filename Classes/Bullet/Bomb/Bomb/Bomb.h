#ifndef _BOMB_H_#define _BOMB_H_#include "cocos2d.h"#include "BombBase.h"USING_NS_CC;class Bomb : public BombBase{public:	static Bomb* createBombWithLevel(int level);    virtual bool initWithLevel(int level);//    CREATE_FUNC(Bomb);     void shoot();    private://	void removeBullet();    void processMonsterDeath(BaseMonster * monster);    void handleFinishBullet();    };#endif