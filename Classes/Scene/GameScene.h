#ifndef _GAME_SCENE_H_#define _GAME_SCENE_H_#include "cocos2d.h"#include "BaseMap.h"#include "PlayerStateMenu.h"#include "TouchLayer.h"USING_NS_CC;enum GameSceneDrawingOrder : unsigned int {    GameSceneDrawingOrderMapLayer = 0,    GameSceneDrawingOrderTouchLayer,    GameSceneDrawingOrderMenuLayer};class GameScene : public Scene {public:	 	virtual bool initGame(int Level, int difficulty); 	static Scene* playGame(int Level, int difficulty);        BaseMap* gameMapLayer;    PlayerStateMenu* playerStateMenu;    TouchLayer* mTouchLayer;private:    void onExit();};#endif